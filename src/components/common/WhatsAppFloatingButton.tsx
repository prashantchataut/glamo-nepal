"use client";
// Client component required: reads route pathname to tailor WhatsApp context and avoid checkout overlap.
import { MessageCircle } from "lucide-react";
import { usePathname } from "next/navigation";
import { SITE_CONFIG } from "@/lib/config";
export function WhatsAppFloatingButton() { const pathname = usePathname(); if (pathname?.startsWith("/checkout")) return null; const isProductPage = pathname?.startsWith("/product/"); const label = isProductPage ? "Ask about this product" : "Chat with us"; const href = isProductPage ? `${SITE_CONFIG.whatsapp}?text=${encodeURIComponent("Hi GLAMO, I have a question about this product.")}` : SITE_CONFIG.whatsapp; return <a href={href} target="_blank" rel="noopener noreferrer" className="group fixed bottom-[calc(var(--mobile-nav-height)+80px)] right-4 z-whatsapp inline-flex h-14 w-14 items-center justify-center rounded-full bg-[#25D366] text-white shadow-2xl shadow-emerald-900/20 transition-transform hover:scale-105 focus:outline-none focus-visible:ring-2 focus-visible:ring-[#25D366] focus-visible:ring-offset-2 md:bottom-24 md:right-6" aria-label={label}><span className="absolute inset-0 rounded-full bg-[#25D366] opacity-35 motion-safe:animate-ping" /><MessageCircle className="relative z-10" size={24} /><span className="pointer-events-none absolute bottom-full right-0 mb-3 hidden whitespace-nowrap rounded-full bg-brand-bgDark px-4 py-2 text-xs font-semibold text-white opacity-0 shadow-lg transition-opacity group-hover:opacity-100 md:block">{label}</span></a>; }
